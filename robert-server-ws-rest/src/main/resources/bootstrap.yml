management:
  endpoint:
    refresh:
      enabled: true
  endpoints:
    web:
      exposure:
        include: refresh
spring:
  application:
    name: robert-server
  cloud:
    config:
      label: default
      uri: ${ROBERT_CONFIG_URI:http://localhost:8888/spring-config}
